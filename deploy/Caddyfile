# Caddyfile — statische hosting voor de Edities-site op een Hetzner-server.
# Caddy regelt automatisch HTTPS (Let's Encrypt). Vervang het domein hieronder.
#
# Installeren (Debian/Ubuntu):
#   sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
#   curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
#   curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
#   sudo apt update && sudo apt install caddy
# Plaats dit bestand als /etc/caddy/Caddyfile en draai:  sudo systemctl reload caddy

edities.example.nl {
	root * /var/www/edities
	encode gzip zstd

	# --- Beheer / bewerkmodus achter een wachtwoord ------------------------
	# De editor is de admin-omgeving: alleen met inlog te openen. De rest van
	# de site (alle edities) blijft openbaar. Genereer de wachtwoord-hash met:
	#   caddy hash-password        (of: caddy hash-password --plaintext 'geheim')
	# en plak de hash hieronder. De gebruikersnaam mag je zelf kiezen.
	@beheer path /editor.html /editor-frame.html
	basicauth @beheer {
		admin $2a$14$VERVANG.DEZE.HASH.DOOR.CADDY.HASH.PASSWORD.UITVOER.abcdefghi
	}

	file_server

	# Code en markup kort cachen zodat deploys snel doorkomen.
	@kort path /assets/* *.html *.txt *.md
	header @kort Cache-Control "public, max-age=300"

	# Afbeeldingen langer cachen (veranderen zelden).
	@afbeeldingen path *.jpg *.jpeg *.png *.svg *.webp
	header @afbeeldingen Cache-Control "public, max-age=604800"
}
